# Runs tests that require a connection to a real QPU

stages:
  - test

default:
  tags:
    - ec2-docker

run-examples:
  stage: test
  image: rust
  script:
    - cargo run --example parametric_compilation
